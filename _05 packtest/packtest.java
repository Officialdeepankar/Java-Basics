//package firstpackage;


/* 
 * To classify one type of class in one directory and another type of classes in other directory we use concepts of packages;
 * commands- 1) javac   filename.java
 * command -2) javac -d . filename.java (this will create a folder that will contain all the classes related ot packtest file )
 */
public class packtest {
    public static void main (String args[])
    {
        System.out.println("Hello!This is deepankar singh!");
    }
}
